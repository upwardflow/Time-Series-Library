# Figure 1 QA

- Core conclusion: recent history carries the full-resolution base-forecast role; distant history carries a compressed correction role.
- Archetype: schematic-led composite with one hero flow and two mechanism insets.
- All labels are schematic abbreviations consistent with the Methods section; tensor dimensions and nonessential layer names are intentionally omitted.
- Input and forecast waveforms are fully contained inside the main panel boundary.
- The RGSP merge uses orthogonal branches with horizontal entry into the summation node.
- RGSP module boxes follow label/function size rather than a uniform template; expanded gaps preserve visible arrow shafts.
- The RGSP summation node and base-forecast output are aligned directly to the right of the variable-graph branch.
- Both mechanism insets use compact content-sized blocks and deliberately longer connector shafts to improve flow perception.
- Compact arrow annotations reuse the manuscript notation for seasonal/trend states, encoded features, DHC condition states, paired outputs, and the correction difference.
- Narrow operator blocks use 90-degree labels (with restrained abbreviations) to free horizontal space while preserving the left-to-right mechanism flow.
- Vertical labels are clockwise-rotated for top-to-bottom reading; enlarged blocks, module text, and edge variables use more of each inset without crossing panel boundaries.
- Publication-scale review drove a second enlargement pass: primary mechanism labels now dominate, while compact edge variables remain visually subordinate.
- The RGSP upper path is horizontally sequenced (patches, Bi-Mamba, variable graph) to avoid stacked vertical labels and improve module separation.
- The DHC inset is reduced to pooling/state extraction, a shared decoder, differencing, and gating.
- All module connectors are straight segments; no nonzero-curvature arrow connection style remains.
- PDF/SVG text remains editable; PNG/TIFF are 600 dpi Python exports.
- Blue denotes the recent/base path, orange the distant/correction path, green normalization/graph operations, and purple shared/control operations.
